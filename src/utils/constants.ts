export enum ROUTES {
  AUTH = 'auth',
  FRIEND_REQUEST = 'friend-request',
}

export enum SERVICES {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  FRIENDS_REQUESTS_SERVICE = 'FRIEND_REQUEST_SERVICE',
}
